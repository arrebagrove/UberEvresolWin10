<ContentDialog
    x:Class="UberEversol.Pages.cdViewSubject"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UberEversol.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Subject Details"
    PrimaryButtonText="Close"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    Opened="ContentDialog_Opened">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="20 10 20 10">
        <Viewbox Width="250" Height="250" HorizontalAlignment="Center" >
            <Image x:Name="imgPerson" Source="{Binding imageObj}" />
        </Viewbox>
        <TextBlock x:Name="txtName" MinWidth="200" HorizontalAlignment="Center" FontWeight="Bold" />
        <TextBlock x:Name="lblUserRating" Text="User Rating" />
        <TextBlock x:Name="txtUserRating" Text="###" />
        <TextBlock x:Name="lblRecordings" Text="Recordings" />
        <TextBlock x:Name="txtRecordings" Text="###" />
    </StackPanel>
</ContentDialog>
